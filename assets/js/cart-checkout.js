/**
 * Скрипт для проверки корзины перед оформлением заказа
 */
document.addEventListener('DOMContentLoaded', function() {
    // Получаем кнопку оформления заказа
    const checkoutBtn = document.getElementById('checkoutBtn');
    
    // Ничего не делаем, просто позволяем кнопке работать как обычная ссылка
    // Проверка корзины будет происходить на странице оформления заказа
});
